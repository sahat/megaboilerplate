extends ../layout

block content
  .login-container.container
    .panel
      .panel-body
        if messages.error
          .alert.alert-danger(role='alert')
            for error in messages.error
              div= error.msg

        form(method='POST')
          legend Log In
          .form-group
            label.control-label(for='email') Email
            input.form-control(type='email', name='email', id='email', placeholder='Email', autofocus=true)
          .form-group
            label.control-label(for='password') Password
            input.form-control(type='password', name='password', id='password', placeholder='Password')
          .form-group
            a(href='/forgot')
              strong Forgot your password?
          button.btn.btn-success(type='submit') Log in

        .hr-title
          span or

        .btn-toolbar.text-center
          //= SIGN_IN_WITH_FACEBOOK_INDENT5
          //= SIGN_IN_WITH_TWITTER_INDENT5
          //= SIGN_IN_WITH_GOOGLE_INDENT5
          //= SIGN_IN_WITH_VK_INDENT5
          //= SIGN_IN_WITH_GITHUB_INDENT5

    p.text-center Don't have an account?&nbsp;
      a(href='/signup')
        strong Sign up
